{% extends "::base.html.twig" %}

{% block title %}AppBundle:Heroku:my{% endblock %}

{% block body %}
<h1>Welcome to the Heroku:my page</h1>
 <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Email</th>
                <th>Mobilephone</th>
              
            </tr>
        </thead>
        <tbody>
        {% for result in results %}
            <tr>
                <td>{{ results.id }}</a></td>
                <td>{{ results.name }}</td>
                <td>{{ results.firstName }}</td>
                <td>{{ results.lastName }}</td>
                <td>{{ results.email }}</td>
                <td>{{ results.mobilePhone }}</td>
                                         </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
